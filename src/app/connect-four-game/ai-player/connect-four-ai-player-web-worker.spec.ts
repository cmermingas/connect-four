/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {ConnectFourAiPlayerWebWorker} from './connect-four-ai-player-web-worker';

describe('ConnectFourAiPlayerWebWorker', () => {
  /*
  let WORKER_FACTORY = require('worker!./connect-four-ai-player-web-worker');
  let client = {
    performMove: function (player: ConnectFourAiPlayerWebWorker) {}
  };

  beforeEach(() => {
    spyOn(client, 'performMove');
  });

  it('should call perform move once', (done) => {
    // TODO: I don't know how to test this web worker yet!

    // let player = new ConnectFourAiPlayerWebWorker(1, WORKER_FACTORY(), client.performMove);
    // player.takeTurn();
    // expect.(client.performMove).toHaveBeenCalledTimes(1);
    done();
  });
  */
});
